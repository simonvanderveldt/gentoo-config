#!/usr/bin/env sh
set -ex

if [ "$(id -u)" != "0" ]; then
  exec sudo -E "$0" "$@"
fi

emerge --ask --verbose --tree --update --newuse --binpkg-respect-use=y --binpkg-changed-deps=y --deep --verbose-conflicts --with-bdeps=y @world
